on: 
  push:
    branches: [main]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        run: echo ${{ secrets.SECRET_TEST }}
      - name: checkout
        uses: actions/checkout@v6
      - name: list_elements
        run: ls -a -l
      